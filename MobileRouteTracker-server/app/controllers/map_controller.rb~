class MapController < ApplicationController

  def index
    @account = Account.where(:accountnr => params[:accountnr], :pass => params[:pass]).first
    @positions = @account.positions
    @last_postion = @account.positions.last
  end

end
